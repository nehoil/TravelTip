export const mapService = {
    gLocations
}

var gLocations = [
    {id, name, lat, lng, weather, createdAt, updatedAt}
];


function getLatLangFromStr(){
    // here goes the geo-api-code
    // Nehoray
    return Promise.resolve({lat,lang})
}